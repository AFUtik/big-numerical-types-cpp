#ifndef ALGO_HPP
#define ALGO_HPP

#include "../bigint.hpp"

/* fast fourier transformation */

extern ntp::bigint fft_mul(const ntp::bigint& a, const ntp::bigint& b) {return {};}

extern ntp::bigint fast_div(const ntp::bigint& a, const ntp::bigint& b) {return {};}

#endif